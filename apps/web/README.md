# Web application

Reserved for the Gemini Antigravity frontend implementation.

Ownership: `apps/web/**` only. The frontend consumes the generated client from `packages/api-client` and must not duplicate backend domain rules.

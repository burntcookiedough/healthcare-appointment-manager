# Infrastructure

Local development starts with PostgreSQL and Redis from the root `compose.yaml`. Hosted infrastructure definitions will be added only after application contracts and deployment targets are finalized.

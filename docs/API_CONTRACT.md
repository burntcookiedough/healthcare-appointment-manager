# API contract

Status: Phase 0 contract draft pending bounded API review.

The API uses `/api/v1`, JSON request/response bodies, UTC timestamps with offsets, stable machine-readable errors, idempotency where retries can create duplicates, and a generated OpenAPI client for the web application.
